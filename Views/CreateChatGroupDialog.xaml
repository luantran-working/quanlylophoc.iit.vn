
<Window x:Class="ClassroomManagement.Views.CreateChatGroupDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Tạo nhóm chat mới" Height="400" Width="300" WindowStartupLocation="CenterOwner">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Tên nhóm:" FontWeight="SemiBold"/>
        <TextBox x:Name="GroupNameBox" Grid.Row="1" Margin="0,5,0,15" Padding="5"/>

        <TextBlock Text="Chọn thành viên:" Grid.Row="2" VerticalAlignment="Top"/>
        <ListBox x:Name="StudentList" Grid.Row="2" Margin="0,20,0,0" SelectionMode="Multiple">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding DisplayName}"/>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="Hủy" Width="70" Margin="0,0,10,0" Click="Cancel_Click"/>
            <Button Content="Tạo nhóm" Width="80" Click="Create_Click" Background="#2563EB" Foreground="White"/>
        </StackPanel>
    </Grid>
</Window>
